# Arquivo técnico preservado sem execução automática.
name: Identidades dos cards main — arquivado

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  arquivo:
    runs-on: ubuntu-latest
    steps:
      - name: Informar estado
        run: echo "Automação arquivada; nenhuma alteração será realizada."
