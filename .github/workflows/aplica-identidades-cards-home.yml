# Fluxo temporário desativado após a aplicação das identidades visuais dos cards.
name: Identidades dos cards — inativo
on:
  workflow_dispatch:
jobs:
  no-op:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Fluxo inativo"
